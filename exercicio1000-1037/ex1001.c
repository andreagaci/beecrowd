//Header

#include <stdio.h>

//Code

int main(){
    //Put the variables
    int num, num2, x;
    //Assign value to variables
    scanf("%d %d", &num, &num2);
    //Convert x to the result of the sum of the variables
    x = num + num2;
    //Print the result
    printf("X = %d\n", x);

    return 0;
}